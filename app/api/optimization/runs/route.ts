import { NextResponse } from 'next/server'
import { createSupabaseServerClient } from '../../../../lib/supabase-server'
import { optimizeSheets, type CuttingPart, type Material } from '../../../../lib/millimetre'

const required=(v:unknown,f:string)=>{const x=String(v??'').trim();if(!x)throw new Error(`${f} is required.`);return x}
const nonNegative=(v:unknown,f:string)=>{const x=Number(v);if(!Number.isFinite(x)||x<0)throw new Error(`${f} must be non-negative.`);return x}

type Placement={partIndex:number;x:number;y:number;width:number;height:number;rotated:boolean}
type Sheet={materialId:string;materialCode:string;width:number;height:number;placements:Placement[]}

export async function GET(request:Request){try{const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Authentication required.'},{status:401});const projectId=new URL(request.url).searchParams.get('projectId');if(!projectId)return NextResponse.json({error:'projectId is required.'},{status:400});const {data,error}=await s.from('millimetre_optimization_runs').select('*').eq('project_id',projectId).order('created_at',{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({optimizationRuns:data??[]})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to load optimization runs.'},{status:500})}}

export async function POST(request:Request){try{const s=await createSupabaseServerClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Authentication required.'},{status:401});const b=await request.json() as Record<string,unknown>;const projectId=required(b.projectId,'projectId'),cuttingListId=required(b.cuttingListId,'cuttingListId'),optimizationCode=required(b.optimizationCode,'optimizationCode');const version=Number(b.version??1);if(!Number.isInteger(version)||version<1)throw new Error('version must be a positive integer.');const kerfMm=nonNegative(b.kerfMm??3,'kerfMm'),trimAllowanceMm=nonNegative(b.trimAllowanceMm??10,'trimAllowanceMm');const {data:cl,error:cle}=await s.from('cutting_lists').select('id,project_id,calculation_run_id,status').eq('id',cuttingListId).maybeSingle();if(cle)throw new Error(cle.message);if(!cl)return NextResponse.json({error:'Cutting list not found.'},{status:404});if(cl.project_id!==projectId||!['approved','issued'].includes(String(cl.status).toLowerCase()))return NextResponse.json({error:'Optimization requires an approved or issued cutting list in the project.'},{status:409});const {data:rows,error:re}=await s.from('cutting_list_items').select('id,part_name,material_id,length_mm,width_mm,thickness_mm,quantity,grain_direction,furniture_item_id').eq('project_id',projectId).eq('calculation_run_id',cl.calculation_run_id).order('sequence_no',{ascending:true});if(re)throw new Error(re.message);if(!rows?.length)return NextResponse.json({error:'Cutting list contains no items.'},{status:409});const materialIds=[...new Set(rows.map(r=>r.material_id).filter(Boolean))];if(!materialIds.length)return NextResponse.json({error:'Cutting list has no material assignments.'},{status:409});const {data:mr,error:me}=await s.from('materials').select('id,code,name,category,thickness_mm,sheet_width_mm,sheet_length_mm,unit_cost').in('id',materialIds);if(me)throw new Error(me.message);if((mr??[]).length!==materialIds.length)return NextResponse.json({error:'One or more materials could not be resolved.'},{status:409});const materials:Material[]=(mr??[]).map(m=>({id:m.id,code:m.code,name:m.name,kind:String(m.category??'board').toLowerCase().replace(/[- ]/g,'_') as Material['kind'],thickness:Number(m.thickness_mm),sheetWidth:Number(m.sheet_width_mm),sheetHeight:Number(m.sheet_length_mm),ratePerSheet:m.unit_cost==null?undefined:Number(m.unit_cost)}));if(materials.some(m=>!(m.thickness>0)||!(m.sheetWidth!>0)||!(m.sheetHeight!>0)))return NextResponse.json({error:'All optimization materials need valid sheet dimensions.'},{status:409});const parts:CuttingPart[]=rows.map(r=>({id:r.id,furnitureId:r.furniture_item_id??projectId,kind:'panel',name:r.part_name,qty:Number(r.quantity),length:Number(r.length_mm),width:Number(r.width_mm),thickness:Number(r.thickness_mm),materialId:r.material_id,grain:String(r.grain_direction??'').toUpperCase()==='REQUIRED',edge:'none',areaSqM:Number(r.length_mm)*Number(r.width_mm)*Number(r.quantity)/1000000,edgeLengthM:0}));const result=optimizeSheets({parts,materials,kerfMm,trimAllowanceMm});if(result.unplaced.length)return NextResponse.json({error:'Optimization could not place every required piece.',unplaced:result.unplaced,result},{status:422});const typedSheets=result.sheets as Sheet[];const sheets=typedSheets.map((sheet,i)=>{const placements=sheet.placements.map(p=>({...p,cuttingListItemId:rows[p.partIndex].id,grainOrientation:rows[p.partIndex].grain_direction==='REQUIRED'?'LENGTH':'NONE'}));const allocatedAreaSqM=placements.reduce((x:number,p:any)=>x+Number(p.width)*Number(p.height)/1000000,0);const area=Number(sheet.width)*Number(sheet.height)/1000000;return{sheetNo:i+1,materialId:sheet.materialId,sheetLengthMm:Number(sheet.width),sheetWidthMm:Number(sheet.height),allocatedAreaSqM,wastagePercent:area?Math.max(0,(area-allocatedAreaSqM)/area*100):0,panelCount:placements.length,cutCount:Math.max(0,placements.length-1),allocation:placements}});const {data:persisted,error:pe}=await s.rpc('persist_optimization_run',{p_project_id:projectId,p_calculation_run_id:cl.calculation_run_id,p_algorithm_version:result.algorithm,p_sheet_count:sheets.length,p_panel_count:sheets.reduce((x,n)=>x+n.panelCount,0),p_cut_count:sheets.reduce((x,n)=>x+n.cutCount,0),p_wastage_percent:result.totalSheetArea?result.wasteArea/result.totalSheetArea*100:0,p_result:{...result,cuttingListId,optimizationCode,version},p_sheets:sheets});if(pe)return NextResponse.json({error:pe.message},{status:409});return NextResponse.json({optimizationRun:persisted,result,createdBy:user.id},{status:201})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Unable to create optimization run.'},{status:400})}}
