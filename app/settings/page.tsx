import { supabase } from '../../lib/supabase'
export default async function SettingsPage(){const {data,error}=await supabase.from('company_settings').select('company_name,currency,timezone').limit(1).maybeSingle();return <main className="main"><header className="top"><div><h1 className="title">Settings</h1><div className="muted">Company and system configuration</div></div><a className="status" href="/">Dashboard</a></header><section className="section"><div className="card"><div className="label">Company</div><div className="value">{error?'Unable to load settings':data?.company_name??'MILLIMETRE'}</div><p className="muted">Currency: {data?.currency??'INR'} · Timezone: {data?.timezone??'Asia/Kolkata'}</p></div></section></main>}