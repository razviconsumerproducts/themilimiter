'use client'

import { useMemo, useState } from 'react'
import { getSupabaseBrowserClient } from '../../lib/supabase-browser'

const statuses=['NEW','CONTACTED','QUALIFIED','DEMO_SCHEDULED','CONVERTED','CLOSED']
const qualifications=['UNQUALIFIED','POTENTIAL','QUALIFIED','HOT']

export default function DemoLeadManager({initialLeads}:{initialLeads:any[]}){
 const [leads,setLeads]=useState(initialLeads); const [selected,setSelected]=useState<any>(null); const [status,setStatus]=useState(''); const [qualification,setQualification]=useState(''); const [notes,setNotes]=useState(''); const [followUp,setFollowUp]=useState(''); const [assignee,setAssignee]=useState(''); const [busy,setBusy]=useState(false); const [msg,setMsg]=useState('')
 const counts=useMemo(()=>Object.fromEntries(statuses.map(s=>[s,leads.filter(l=>l.status===s).length])),[leads])
 const overdue=useMemo(()=>leads.filter(l=>l.next_follow_up_at && new Date(l.next_follow_up_at)<new Date() && !['CLOSED','CONVERTED'].includes(l.status)),[leads])
 async function save(){if(!selected)return;setBusy(true);setMsg('');const supabase=getSupabaseBrowserClient();const {data,error}=await supabase.rpc('manage_demo_lead',{p_lead_id:selected.id,p_status:status||null,p_assigned_to:assignee||null,p_notes:notes||null,p_next_follow_up_at:followUp?new Date(followUp).toISOString():null,p_qualification:qualification||null});if(error){setMsg(error.message);setBusy(false);return}setLeads(prev=>prev.map(l=>l.id===data.id?data:l));setSelected(data);setNotes(data.notes||'');setStatus(data.status);setQualification(data.qualification||'');setAssignee(data.assigned_to||'');setFollowUp(data.next_follow_up_at?new Date(data.next_follow_up_at).toISOString().slice(0,16):'');setMsg('Lead updated');setBusy(false)}
 function selectLead(l:any){setSelected(l);setStatus(l.status||'NEW');setQualification(l.qualification||'UNQUALIFIED');setAssignee(l.assigned_to||'');setFollowUp(l.next_follow_up_at?new Date(l.next_follow_up_at).toISOString().slice(0,16):'');setNotes(l.notes||'');setMsg('')}
 return <><section className="metric-grid">{statuses.map(s=><button className="metric-card" style={{textAlign:'left',cursor:'pointer'}} onClick={()=>setStatus(s)} key={s}><span>{s.replace('_',' ')}</span><strong>{counts[s]}</strong><small>Filter / set status</small></button>)}</section>{overdue.length>0&&<section className="panel" style={{marginBottom:18,borderLeft:'4px solid #b91c1c'}}><strong>⚠ {overdue.length} overdue follow-up{overdue.length===1?'':'s'}</strong><p style={{margin:'6px 0 0'}}>These active leads need attention.</p></section>}<section className="content-grid"><div className="panel"><div className="panel-head"><div><h2>Incoming requests</h2><p>{leads.length} latest requests</p></div></div><div className="project-list">{leads.map(l=><button key={l.id} onClick={()=>selectLead(l)} style={{display:'flex',width:'100%',textAlign:'left',justifyContent:'space-between',gap:20,padding:'16px 0',border:0,borderBottom:'1px solid var(--line,#e5e7eb)',background:'transparent',cursor:'pointer'}}><span><b>{l.name}</b><span style={{display:'block'}}>{l.company||'No company'} · {l.email}</span><small>{l.phone||''}{l.next_follow_up_at?` · Follow-up ${new Date(l.next_follow_up_at).toLocaleString('en-IN')}`:''}</small></span><em>{l.status}</em></button>)}</div></div>{selected&&<aside className="panel"><div className="panel-head"><div><h2>{selected.name}</h2><p>{selected.company||'No company'} · {selected.email}</p></div><button className="ghost-btn" onClick={()=>setSelected(null)}>Close</button></div><p>{selected.message||'No message provided.'}</p><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></label><label>Qualification<select value={qualification} onChange={e=>setQualification(e.target.value)}>{qualifications.map(q=><option key={q}>{q}</option>)}</select></label><label>Assigned owner<input value={assignee} onChange={e=>setAssignee(e.target.value)} placeholder="Team member user ID"/></label><label>Next follow-up<input type="datetime-local" value={followUp} onChange={e=>setFollowUp(e.target.value)}/></label><label>Notes<textarea rows={7} value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Qualification notes, next action, meeting details..."/></label><button className="primary-btn" onClick={save} disabled={busy}>{busy?'Saving…':'Save lead'}</button>{msg&&<div className="empty" style={{marginTop:12}}>{msg}</div>}</aside>}</section></>
}
