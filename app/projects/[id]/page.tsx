import { notFound } from 'next/navigation'
import { createSupabaseServerClient } from '../../../lib/supabase-server'
export default async function ProjectDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createSupabaseServerClient();const {data:project,error}=await s.from('projects').select('id,customer_id,name,code,status,current_stage,site_address,notes,created_at,updated_at').eq('id',id).maybeSingle();if(error||!project)notFound();const links=[['Measurements','/measurements'],['Furniture','/furniture'],['Calculation','/calculation'],['Cutting List','/cutting-list'],['BOM / BOQ','/bom'],['Optimization','/optimization'],['Costing','/costing'],['Quotation','/quotation'],['Approval / Payment','/payments'],['Purchase','/purchasing'],['Goods Receipt + QC','/receiving'],['Inventory','/inventory'],['Production','/manufacturing'],['Production QC','/quality'],['Labels / QR','/labels'],['Delivery','/delivery'],['Installation','/installation'],['Handover','/handover'],['Warranty / Service','/service']];return <main className="main"><header className="top"><div><h1 className="title">{project.name}</h1><div className="muted">{project.code} · {project.status} · {project.current_stage??'Stage not set'}</div></div><a className="status" href="/projects">All Projects</a></header><section className="section"><div className="card"><h2>Project Control</h2><p><b>Customer:</b> {project.customer_id}</p><p><b>Site:</b> {project.site_address??'—'}</p><p>{project.notes??'No project notes.'}</p></div><div className="card"><h2>Canonical Workflow</h2><div className="feature-grid">{links.map(([label,href])=><a className="feature-card" href={href} key={href}><h3>{label}</h3><p>Open stage</p></a>)}</div></div></section></main>}
