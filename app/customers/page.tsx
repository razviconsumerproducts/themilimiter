import { supabase } from '../../lib/supabase'
export default async function CustomersPage(){const {data:rows,error}=await supabase.from('customers').select('name,email,phone,address,active,created_at').order('name');return <main className="main"><header className="top"><div><h1 className="title">Customers</h1><div className="muted">Customer master</div></div><a className="status" href="/">Dashboard</a></header>{error?<div className="card">Unable to load customers.</div>:<section className="section"><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Phone</th><th>Address</th><th>Status</th></tr></thead><tbody>{(rows??[]).map((r:any,i:number)=><tr key={i}><td>{r.name}</td><td>{r.email??'—'}</td><td>{r.phone??'—'}</td><td>{r.address??'—'}</td><td>{r.active?'Active':'Inactive'}</td></tr>)}</tbody></table></section>}</main>}