import { createSupabaseServerClient } from '../../lib/supabase-server'

export default async function SalesPage(){const supabase=await createSupabaseServerClient();const {data:orders,error}=await supabase.from('sales_orders').select('order_no,status,total,created_at,customers(name)').order('created_at',{ascending:false}).limit(100);return <main className="main"><header className="top"><div><h1 className="title">Sales</h1><div className="muted">Customer sales orders</div></div><a className="status" href="/">Dashboard</a></header><section className="section"><div className="card"><h2>Sales Orders</h2>{error?<p role="alert">Unable to load sales orders.</p>:<table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Total</th><th>Created</th></tr></thead><tbody>{(orders??[]).map((o:any)=><tr key={o.order_no}><td>{o.order_no}</td><td>{o.customers?.name??'—'}</td><td>{o.status??'—'}</td><td>₹{Number(o.total??0).toLocaleString('en-IN')}</td><td>{o.created_at?new Date(o.created_at).toLocaleDateString('en-IN'):'—'}</td></tr>)}{!(orders??[]).length&&<tr><td colSpan={5}>No sales orders found.</td></tr>}</tbody></table>}</div></section></main>}
