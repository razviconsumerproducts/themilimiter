import { createSupabaseServerClient } from '../../lib/supabase-server'
export default async function PaymentsPage(){const s=await createSupabaseServerClient();const {data,error}=await s.from('millimetre_quotations').select('id,project_id,quotation_no,version,currency,grand_total,status,quotation_date,valid_until').order('created_at',{ascending:false}).limit(100);return <main className="main"><header className="top"><div><h1 className="title">Approval / Payment</h1><div className="muted">Stage 11 — customer commercial gate before purchasing</div></div><a className="status" href="/purchasing">Purchase →</a></header><section className="section"><div className="card"><h2>Commercial Approval Queue</h2><p className="muted">Quotation status is the source of truth for the customer commercial gate. Purchasing remains blocked until its own approval controls are satisfied.</p>{error?<p role="alert">Unable to load approval queue: {error.message}</p>:<table className="table"><thead><tr><th>Quotation</th><th>Version</th><th>Project</th><th>Amount</th><th>Status</th><th>Valid until</th></tr></thead><tbody>{(data??[]).map((q:any)=><tr key={q.id}><td>{q.quotation_no}</td><td>v{q.version}</td><td>{q.project_id}</td><td>{q.currency} {Number(q.grand_total??0).toLocaleString('en-IN')}</td><td>{q.status}</td><td>{q.valid_until??'—'}</td></tr>)}{!(data??[]).length&&<tr><td colSpan={6}>No quotations awaiting approval.</td></tr>}</tbody></table>}</div></section></main>}
