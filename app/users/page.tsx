import { createSupabaseServerClient } from '../../lib/supabase-server'

export default async function UsersPage(){const supabase=await createSupabaseServerClient();const {data:roles,error}=await supabase.from('user_roles').select('user_id,role,created_at').order('created_at',{ascending:false}).limit(100);return <main className="main"><header className="top"><div><h1 className="title">Users & Roles</h1><div className="muted">Application role assignments</div></div><a className="status" href="/">Dashboard</a></header><section className="section"><div className="card"><h2>Your role access</h2>{error?<p role="alert">Unable to load role information.</p>:<table className="table"><thead><tr><th>User ID</th><th>Role</th><th>Created</th></tr></thead><tbody>{(roles??[]).map((r:any)=><tr key={`${r.user_id}-${r.role}`}><td>{r.user_id}</td><td>{r.role??'—'}</td><td>{r.created_at?new Date(r.created_at).toLocaleDateString('en-IN'):'—'}</td></tr>)}{!(roles??[]).length&&<tr><td colSpan={3}>No role assignments found.</td></tr>}</tbody></table>}</div></section></main>}
