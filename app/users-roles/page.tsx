import { supabase } from '../../lib/supabase'
export default async function UsersRolesPage(){const {data:{user}}=await supabase.auth.getUser();const {data:role}=user?await supabase.from('user_roles').select('role').eq('user_id',user.id).maybeSingle():{data:null};return <main className="main"><header className="top"><div><h1 className="title">Users & Roles</h1><div className="muted">Authenticated access</div></div><a className="status" href="/">Dashboard</a></header><section className="section"><div className="card"><div className="label">Current user</div><div className="value">{user?.email??'Not signed in'}</div><p className="muted">Role: {role?.role??'operator'}</p></div></section></main>}